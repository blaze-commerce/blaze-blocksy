<?php
/**
 * {Client Name} — Client-specific hooks.
 *
 * Loaded via manifest.json when this client is active.
 * Only add hooks specific to THIS client here.
 * Reusable hooks go in inc/woocommerce.php or inc/hooks.php.
 *
 * Common client-specific hooks:
 * - Related products heading text
 * - Review collection shortcode + ID
 * - Payment icons for client's gateways
 * - Custom gettext translations
 *
 * @package Blocksy_Child
 * @client  {Client Name}
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Add client-specific hooks below.
